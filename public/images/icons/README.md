# `public/images/icons`

## Purpose

Static icon assets referenced by public URL (non-Lucide custom icons).

## Where images belong

Place custom SVG/PNG icons here.

Register paths in `src/config/images.ts` under `images.icons`.

Prefer Lucide React for UI icons when a suitable icon exists.
