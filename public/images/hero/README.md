# `public/images/hero`

## Purpose

Hero and large banner imagery for landing/header treatments.

## Where images belong

Place homepage and page-level hero photos here (e.g. `home-hero.webp`).

Register paths in `src/config/images.ts` under `images.hero`.
