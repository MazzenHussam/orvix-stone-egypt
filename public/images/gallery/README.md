# `public/images/gallery`

## Purpose

Gallery album and project showcase images.

## Where images belong

Place gallery photos here, optionally grouped by album subfolders later.

Register paths in `src/config/images.ts` under `images.gallery`.
